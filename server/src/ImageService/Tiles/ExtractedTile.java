package ImageService.Tiles;

import ImageService.ImageTile;
import org.opencv.core.Mat;

public class ExtractedTile extends ImageTile {
    public ExtractedTile(Mat img) {
        super(img);
    }
}
